name: verify

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"
      - name: Suite integrity check
        run: python mindseam/scripts/verify_suite.py
      - name: Metric layer audit
        run: python tools/metric_audit.py --check --samples 400
      - name: Controller regression tests
        run: python -m unittest discover -s tests -v
